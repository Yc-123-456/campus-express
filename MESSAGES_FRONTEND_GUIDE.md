# 私信功能 - 前端使用说明

## 一、功能概述

私信功能为用户提供了以下能力：

1. **用户间私信**: 发布者和接单人可以通过私信功能进行沟通
2. **系统消息**: 系统自动向用户推送订单状态变更信息
3. **会话管理**: 左侧显示所有会话列表，右侧显示聊天窗口
4. **未读提示**: 导航栏显示未读消息数量角标
5. **消息过滤**: 支持查看全部会话或仅查看未读会话

## 二、页面结构

### 1. 消息页面 (messages.html)

页面分为左右两部分：

**左侧 - 会话列表**:
- 显示所有会话（包括用户私信和系统消息）
- 每个会话显示：
  - 对方用户头像（首字母）
  - 对方用户名
  - 最后一条消息预览
  - 最后消息时间
  - 未读消息数角标
- 支持过滤器：全部 / 未读

**右侧 - 聊天窗口**:
- 显示选中会话的聊天记录
- 顶部显示对方用户信息
- 中间显示消息列表（区分发送/接收/系统消息）
- 底部提供消息输入框和发送按钮

### 2. 导航栏集成

在所有页面的导航栏中都添加了"我的消息"链接，并显示未读消息数量角标。

## 三、使用流程

### 1. 查看消息

1. 点击导航栏的"我的消息"进入消息页面
2. 左侧显示所有会话列表
3. 点击某个会话，右侧显示该会话的聊天记录
4. 查看消息时会自动标记为已读

### 2. 发送消息

1. 在会话列表中选择一个会话
2. 在底部输入框中输入消息内容
3. 点击"发送"按钮或按回车键发送
4. 消息发送成功后会立即显示在聊天窗口中

### 3. 系统消息

当订单状态发生变化时，系统会自动发送消息通知相关用户：

- **订单被接取**: 通知发布者
- **订单开始配送**: 通知发布者
- **订单已送达**: 通知发布者确认收货
- **订单完成**: 通知接单人
- **订单取消**: 通知相关方

系统消息在聊天窗口中以灰色气泡居中显示，与普通消息区分开来。

### 4. 未读消息提示

- 导航栏的"我的消息"链接上会显示红色角标，显示未读消息总数
- 会话列表中，有未读消息的会话会显示未读数角标
- 进入消息页面后，未读消息会自动标记为已读

## 四、技术实现

### 1. 文件清单

```
campus-front/
├── messages.html              # 消息页面
├── js/
│   ├── messages.js            # 消息功能模块
│   └── common.js              # 公共函数（包含加载未读数功能）
└── MESSAGES_BACKEND_GUIDE.md  # 后端实现指南
```

### 2. 核心功能模块

#### messages.js 主要函数:

| 函数名 | 功能说明 |
|--------|---------|
| `loadConversations()` | 加载会话列表 |
| `renderConversations()` | 渲染会话列表UI |
| `selectConversation()` | 选择会话，加载聊天记录 |
| `loadChatMessages()` | 加载聊天消息 |
| `renderMessages()` | 渲染消息列表UI |
| `handleSendMessage()` | 处理发送消息 |
| `markConversationAsRead()` | 标记会话为已读 |
| `loadUnreadCount()` | 加载未读消息数量 |
| `updateUnreadBadge()` | 更新导航栏未读角标 |

#### common.js 新增函数:

| 函数名 | 功能说明 |
|--------|---------|
| `loadNavUnreadCount()` | 加载并更新导航栏未读角标 |
| `updateNavUnreadBadge()` | 更新指定元素的未读角标显示 |

### 3. API 接口调用

前端调用的后端接口：

```javascript
// 获取会话列表
GET /api/messages/conversations?page=1&pageSize=20&type=all

// 获取会话消息
GET /api/messages/conversation/{userId}?page=1&pageSize=50

// 发送消息
POST /api/messages/send
Body: { receiverId, content, orderId? }

// 标记会话为已读
POST /api/messages/conversation/{userId}/read

// 获取未读数量
GET /api/messages/unread-count
```

### 4. 样式说明

消息页面使用了自定义 CSS 样式：

- `.messages-container`: 主容器，设置固定高度
- `.conversations-list`: 会话列表容器，支持滚动
- `.conversation-item`: 单个会话项，hover 和 active 状态
- `.chat-window`: 聊天窗口，flex 布局
- `.message-item`: 消息项，区分 sent/received/system
- `.message-bubble`: 消息气泡样式
- `.unread-badge`: 未读角标样式

## 五、注意事项

### 1. 认证要求

- 所有消息相关操作都需要用户登录
- 未登录用户访问消息页面会自动跳转到登录页
- 所有 API 请求都需要携带 JWT Token

### 2. 权限控制

- 用户只能查看和操作自己的消息
- 不能查看其他用户的私密消息
- 系统消息对所有用户可见

### 3. 性能优化

- 会话列表和消息列表都使用分页加载
- 每30秒自动刷新一次未读消息数
- 消息内容过长时会截断显示

### 4. 兼容性

- 支持现代浏览器（Chrome, Firefox, Safari, Edge）
- 使用 Bootstrap 5 响应式布局
- 移动端自适应

### 5. 安全考虑

- 前端对消息内容进行 HTML 转义，防止 XSS 攻击
- 所有敏感操作都需要后端验证权限
- Token 存储在 localStorage，注意防范 XSS

## 六、扩展建议

### 1. 实时推送

当前使用轮询方式获取未读消息，如需实时推送可考虑：

- 使用 WebSocket 实现双向通信
- 使用 Server-Sent Events (SSE)
- 使用第三方推送服务

### 2. 多媒体消息

如需支持图片、文件等多媒体消息：

- 扩展数据库表结构，添加消息类型字段
- 实现文件上传接口
- 前端添加文件选择和预览功能

### 3. 消息搜索

添加消息搜索功能：

- 后端提供搜索接口，支持关键词搜索
- 前端添加搜索框和搜索结果展示
- 支持按时间范围筛选

### 4. 消息撤回

实现消息撤回功能：

- 添加消息撤回状态字段
- 限制撤回时间（如2分钟内）
- 前端显示"已撤回"提示

### 5. 离线消息

支持离线消息推送：

- 记录用户最后在线时间
- 用户上线时推送离线期间的消息
- 可选邮件或短信通知

## 七、测试建议

### 1. 功能测试

- [ ] 会话列表正常加载
- [ ] 点击会话能正确显示聊天记录
- [ ] 发送消息功能正常
- [ ] 未读消息数正确显示
- [ ] 标记已读功能正常
- [ ] 系统消息正确显示
- [ ] 过滤器功能正常

### 2. 边界测试

- [ ] 无会话时的空状态显示
- [ ] 消息内容为空时的提示
- [ ] 消息过长时的显示效果
- [ ] 网络异常时的错误提示
- [ ] 并发发送消息的处理

### 3. 兼容性测试

- [ ] 不同浏览器下的显示效果
- [ ] 移动端适配
- [ ] 不同屏幕分辨率下的布局

## 八、常见问题

### Q1: 未读消息数不更新？

**A**: 检查以下几点：
1. 后端接口是否正常返回数据
2. Token 是否正确传递
3. 浏览器控制台是否有错误信息
4. 尝试手动刷新页面

### Q2: 发送消息失败？

**A**: 检查以下几点：
1. 是否选择了会话
2. 消息内容是否为空
3. 网络连接是否正常
4. 后端接口是否正常运行

### Q3: 会话列表为空？

**A**: 可能原因：
1. 确实没有会话（新用户）
2. 后端接口返回数据格式不正确
3. 权限问题导致无法获取数据

### Q4: 消息显示顺序不对？

**A**: 检查：
1. 后端返回的消息是否按时间排序
2. 前端渲染逻辑是否正确
3. 时间戳格式是否正确

## 九、维护建议

1. **定期清理**: 建议定期清理过期消息，避免数据库过大
2. **监控日志**: 记录消息发送失败的日志，便于排查问题
3. **性能监控**: 监控接口响应时间，优化慢查询
4. **用户反馈**: 收集用户使用反馈，持续优化体验
